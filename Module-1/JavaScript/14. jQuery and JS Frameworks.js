$('#registerBtn').click(() => {
  console.log("Registered via jQuery");
});

$('.eventCard').fadeIn();
$('.eventCard').fadeOut();

// One benefit of using React/Vue:
// Components re-render dynamically, making UI updates more efficient and scalable.

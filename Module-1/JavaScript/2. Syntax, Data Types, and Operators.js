const eventName = "Community Music Fest";
const eventDate = "2025-07-20";
let seats = 50;

console.log(`${eventName} is happening on ${eventDate}. Seats available: ${seats}`);

seats--; // When someone registers
console.log(`Seats left: ${seats}`);
